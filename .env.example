# PostgreSQL Configuration
POSTGRES_PASSWORD=postgres
POSTGRES_USER=postgres
POSTGRES_DB=postgres
POSTGRES_PORT=5432

# Railway Configuration (for CI/CD deployment)
# Get your Railway token from: https://railway.app/account/tokens
# IMPORTANT: Never commit your actual Railway token to version control!
# Add it as a GitHub secret: Settings > Secrets and variables > Actions > New repository secret
RAILWAY_TOKEN=your_railway_api_token_here

# Optional: Railway Service ID (if you have multiple services in your project)
# Find this in your Railway service settings URL or via Railway CLI
RAILWAY_SERVICE_ID=postgres
